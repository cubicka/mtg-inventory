{
  "name": "inventory",
  "version": "1.0.0",
  "description": "",
  "main": "imageFetcher.js",
  "scripts": {
    "build": "babel src --out-dir dist",
    "start": "npm run build && node dist/server/server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.17.4"
  }
}
